import 'package:flutter/material.dart';

class ChooseQuizPage extends StatelessWidget {
  const ChooseQuizPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('Choose Quiz Page'),
    );
  }
}
